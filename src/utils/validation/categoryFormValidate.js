const categoryFormValidate = {
  validate: {
    input: () => 'Имя категории обязательное поле',
    select: () => 'Выбор категории обязательное поле',
  },
};

export const { validate } = categoryFormValidate;
